#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main(void){
  int n;

  while(cin >> n && n){
    vector<int> time;
    int tmp;
    int time=0;
    for(int i=0;i<n;i++){
      cin >> tmp;
      time.push_back();
    }
  }
}
