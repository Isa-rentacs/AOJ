#include <iostream>
#include <algorithm>
using namespace std;

int main(void){
  int n;
  while(cin >> n && n){
    int a[n],sum,ret = 1<<29;
    for(int i=0;i<n;i++) cin >> a[i];
    sum = accumulate(a,a+n,0);
    for(int i=0;i<(1<<21);i++){
      int tmp=0
    }
  }
}
