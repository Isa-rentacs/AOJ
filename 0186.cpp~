#include <iostream>
#include <algorithm>

using namespace std;

int main(){
    int q1;
    
    while(cin >> q1){
        int badget, pi, pn, lim;
        int reti, retn;
        cin >> badget >> pi >> pn >> lim;
        
        if(pi*q1 <= badget){

        }
    }
}
