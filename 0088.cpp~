#include <iostream>
#include <string>

using namespace std;

int main(void){
  string str, ret;

  while(getline(cin, str)){
    ret = "";
    for(int i=0;i<str.size();i++){
      if(str[i] == ' ')
    }
  }
}
