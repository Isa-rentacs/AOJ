#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <cstring>

using namespace std;

int main(void){
  int n;
  while(cin >> n && n){
    bool table[40][40]; 
    vector< pair<int,string> > person(n);
    memset(table, false, sizeof(table));
    int d,tmp;

    for(int i=0;i<n;i++){
      person[i].first = 0;
      cin >> person[i].second >> d;
      for(int j=0;j<d;j++){
	cin
      }
    }
    
  }
}
