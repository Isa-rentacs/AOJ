#include <iostream>
#include <cstring>
#include <cmath>
#include <cstdlib>
#include <string>
#include <sstream>
#include <vector>

using namespace std;

int main(void){
  string str;

  while(getline(cin, str)){
    istringstream iss(str);
    double l;
    vector<int> cs;
    iss >> l;
    
    
  }
}
